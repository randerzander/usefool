# OpenRouter API key (required)
OPENROUTER_API_KEY=your_api_key_here

# GitHub Personal Access Token (optional - for scraping GitHub URLs without rate limiting)
# Create at: https://github.com/settings/tokens
# Put the token in .github_token file (one line, no quotes)

# Proxy whitelist file for pysearx (optional - improves search reliability)
# Point to a file containing proxy URLs, one per line
# Example: PROXY_FILE=whitelist_2026-01-11_15.txt
# PROXY_FILE=
